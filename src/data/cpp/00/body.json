{
  "bodyType": "mcq",
  "questions": [
    {
      "id": 1,
      "text": "Which command compiles `program.cpp` and names the output `run`?",
      "options": {
        "A": "`g++ -o run program.cpp`",
        "B": "`g++ program.cpp run`",
        "C": "`g++ program.cpp --output run`",
        "D": "`g++ run program.cpp -compile`"
      },
      "correct": ["A"],
      "mode": "any",
      "explanation": "The **`-o` flag** tells `g++` the name of the output file.\n\nThe correct syntax is:\n```cpp\ng++ -o run program.cpp\n./run\n```\n\n- `-o run` means \"name the output `run`\"\n- Without `-o`, the output is called `a.out` by default\n\nThe other options use wrong flags.\n`g++` does not have `--output` or `-compile`."
    },
    {
      "id": 2,
      "text": "What is the output?\n```cpp\nint a = 5, b = 8;\nif (a > 6 && b > 6) std::cout << \"X\";\nelse if (a < 6 && b > 6) std::cout << \"Y\";\nelse std::cout << \"Z\";\n```",
      "options": {
        "A": "Z",
        "B": "Y",
        "C": "X",
        "D": "Compilation error"
      },
      "correct": ["B"],
      "mode": "any",
      "explanation": "We have `a = 5` and `b = 8`. Trace step by step:\n\n- `a > 6 && b > 6` → `5 > 6` is `false` → the whole `&&` is `false`\n- `a < 6 && b > 6` → `5 < 6` is `true` AND `8 > 6` is `true` → prints `\"Y\"`\n\n**`&&` (AND)** needs both sides to be `true`.\n`||` (OR) needs only one side to be `true`.\n\n`&&` uses **short-circuit evaluation** — if the left side is `false`, it does not check the right side."
    },
    {
      "id": 3,
      "text": "What is the output?\n```cpp\nint x = 5;\nvoid foo() { int x = 10; std::cout << x; }\nint main() { foo(); std::cout << x; }\n```",
      "options": {
        "A": "55",
        "B": "1010",
        "C": "105",
        "D": "Compilation error"
      },
      "correct": ["C"],
      "mode": "any",
      "explanation": "There are two variables named `x` — one global, one local to `foo()`.\n\nStep by step:\n- `foo()` has its own `int x = 10`. This **shadows** (hides) the global `x = 5`.\n- So `std::cout << x` inside `foo()` prints `10`.\n- `main()` has no local `x`, so it uses the global `x = 5`. Prints `5`.\n\nOutput: `105`.\n\n**Scope** decides which `x` is used. The local variable always wins over the global one.\nTo reach the global `x` from inside `foo()`, write `::x`."
    },
    {
      "id": 4,
      "text": "Which type should you use to store `true` or `false`?",
      "options": {
        "A": "`int`",
        "B": "`float`",
        "C": "`char`",
        "D": "`bool`"
      },
      "correct": ["D"],
      "mode": "any",
      "explanation": "**`bool`** is the type for `true` or `false` in C++.\n\n```cpp\nbool passed = true;\nbool isEven = (n % 2 == 0);\n```\n\n- `int`, `char`, and `float` can also hold 0 or 1, but they are not meant for this.\n- `bool` makes your code clear. Other programmers see it and know: this is a yes/no value.\n\nRule: always use `bool` when the variable means true or false."
    },
    {
      "id": 5,
      "text": "What is the output?\n```cpp\nint x = 4, y = 4;\nif (x == 4 && y == 5) std::cout << \"A\";\nelse if (x == 4 || y == 5) std::cout << \"B\";\nelse std::cout << \"C\";\n```",
      "options": {
        "A": "B",
        "B": "A",
        "C": "C",
        "D": "Compilation error"
      },
      "correct": ["A"],
      "mode": "any",
      "explanation": "We have `x = 4` and `y = 4`. Trace each condition:\n\n- `x == 4 && y == 5` → `true && false` → `false`. **`&&`** needs both sides `true`.\n- `x == 4 || y == 5` → `true || false` → `true`. **`||`** needs only one side `true`.\n\nThe second branch runs and prints `\"B\"`.\n\nBe careful: the output text is `\"B\"`, but the answer choice is **A**. Do not confuse them."
    },
    {
      "id": 6,
      "text": "What is the output?\n```cpp\nvoid triple(int x) { x = x * 3; }\nint main() {\n    int n = 4;\n    triple(n);\n    std::cout << n;\n}\n```",
      "options": {
        "A": "12",
        "B": "4",
        "C": "Compilation error",
        "D": "Undefined behavior"
      },
      "correct": ["B"],
      "mode": "any",
      "explanation": "This is **pass by value** — the function gets a copy, not the original.\n\nStep by step:\n- `n = 4` in `main()`\n- `triple(n)` sends a copy of `4`\n- Inside the function, only the copy changes to `12`\n- `n` stays `4`\n\nTo change the original, use a reference:\n```cpp\nvoid triple(int& x) { x = x * 3; }\n```"
    },
    {
      "id": 7,
      "text": "How many times is \"ok\" printed?\n```cpp\nint i = 0;\nwhile (i < 4) {\n    std::cout << \"ok\\n\";\n    i++;\n}\n```",
      "options": {
        "A": "3",
        "B": "5",
        "C": "4",
        "D": "0"
      },
      "correct": ["C"],
      "mode": "any",
      "explanation": "The `while` loop checks the condition before each round.\n\nTrace step by step:\n- `i = 0` → `0 < 4` is true → print \"ok\" → `i` becomes `1`\n- `i = 1` → `1 < 4` is true → print \"ok\" → `i` becomes `2`\n- `i = 2` → `2 < 4` is true → print \"ok\" → `i` becomes `3`\n- `i = 3` → `3 < 4` is true → print \"ok\" → `i` becomes `4`\n- `i = 4` → `4 < 4` is false → stop\n\n**4 times** total.\n\nUseful rule: `while (i < N)` starting from `i = 0` always runs exactly `N` times."
    },
    {
      "id": 8,
      "text": "What is the output?\n```cpp\nnamespace Config { int version = 2; }\nint version = 5;\nint main() {\n    int version = 8;\n    std::cout << version << \" \" << ::version << \" \" << Config::version;\n}\n```",
      "options": {
        "A": "5 8 2",
        "B": "2 5 8",
        "C": "8 2 5",
        "D": "8 5 2"
      },
      "correct": ["D"],
      "mode": "any",
      "explanation": "There are three different `version` variables. Each one lives in a different **scope**.\n\nStep by step:\n- `version` (no prefix) → finds the local variable in `main()` → `8`\n- `::version` → `::` means \"global namespace\" → `5`\n- `Config::version` → looks inside the `Config` namespace → `2`\n\nOutput: `8 5 2`.\n\nThe **scope resolution operator** `::` lets you choose which `version` you mean.\n- `::name` = global\n- `Namespace::name` = inside that namespace"
    },
    {
      "id": 9,
      "text": "What is the output?\n```cpp\nint* p = new int(3);\nint* q = new int(7);\ndelete p;\np = q;\nstd::cout << *p;\n```",
      "options": {
        "A": "7",
        "B": "3",
        "C": "Undefined behavior",
        "D": "Compilation error"
      },
      "correct": ["A"],
      "mode": "any",
      "explanation": "Step by step:\n- `p` points to `3` on the heap. `q` points to `7` on the heap.\n- `delete p` frees the memory holding `3`.\n- `p = q` makes `p` point to the same place as `q` (the `7`).\n- `*p` reads valid memory → prints `7`.\n\nThis is safe. The `7` was **not deleted**, so reading it is fine.\n\nBe careful: now both `p` and `q` point to the same memory. You must only **`delete` it once**. Deleting both would be a double-free bug."
    },
    {
      "id": 10,
      "text": "What is the output?\n```cpp\ndouble result = 9 / 4;\nstd::cout << result;\n```",
      "options": {
        "A": "2.25",
        "B": "2",
        "C": "2.5",
        "D": "Compilation error"
      },
      "correct": ["B"],
      "mode": "any",
      "explanation": "Both `9` and `4` are `int`. When both sides are `int`, C++ does **integer division** — it throws away the decimal part.\n\nSo `9 / 4` = `2`, not `2.25`.\n\nStoring `2` into a `double` gives `2.0`, but the `.25` is already lost.\n\n```cpp\ndouble a = 9 / 4;    // integer division → 2\ndouble b = 9.0 / 4;  // floating-point division → 2.25\n```\n\nRule: if **either** side is `double` or `float`, you get floating-point division."
    },
    {
      "id": 11,
      "text": "`int` has a maximum value of 2,147,483,647. What is the output?\n```cpp\nint x = 2147483647;\nx = x + 1;\nstd::cout << x;\n```",
      "options": {
        "A": "2147483648",
        "B": "0",
        "C": "-2147483648",
        "D": "Compilation error"
      },
      "correct": ["C"],
      "mode": "any",
      "explanation": "`int` has a maximum value of 2,147,483,647. Adding `1` goes past the limit.\n\nThis is **signed integer overflow**, which is **undefined behavior** in C++.\n\nOn most computers, the number wraps around to `-2147483648`. This is because of how **two's complement** stores numbers in binary:\n- `0111...1111` + 1 = `1000...0000`\n- In two's complement, `1000...0000` is the most negative number.\n\nBut you should never depend on this wrapping. The compiler is allowed to do anything when overflow happens."
    },
    {
      "id": 12,
      "text": "What is the output?\n```cpp\nint x = 7;\nif (x = 1) std::cout << \"A\";\nelse std::cout << \"B\";\n```",
      "options": {
        "A": "B",
        "B": "The program crashes at runtime",
        "C": "Compilation error",
        "D": "A"
      },
      "correct": ["D"],
      "mode": "any",
      "explanation": "The bug: `=` is **assignment**, not comparison. `==` is comparison.\n\nWhat happens:\n- `x = 1` assigns `1` to `x` and returns `1`\n- `1` is nonzero, so the `if` condition is `true`\n- Prints `\"A\"`\n\nThe fix:\n```cpp\nif (x = 1)   // BUG: assigns 1 to x, always true\nif (x == 1)  // CORRECT: compares x to 1\n```\n\nThis is a very common mistake. Most compilers will give a **warning** if you write `=` inside an `if`."
    },
    {
      "id": 13,
      "text": "What is the output?\n```cpp\nint x = 10;\n{\n    int x = 20;\n    std::cout << x;\n}\nstd::cout << x;\n```",
      "options": {
        "A": "2010",
        "B": "1020",
        "C": "2020",
        "D": "1010"
      },
      "correct": ["A"],
      "mode": "any",
      "explanation": "Each pair of `{}` creates a new **block scope**.\n\nStep by step:\n- Inside `{}`, a new `int x = 20` is created. It **shadows** the outer `x = 10`.\n- `std::cout << x` prints `20` (the inner one).\n- The `}` ends the block. The inner `x` is destroyed.\n- Now only the outer `x = 10` exists. `std::cout << x` prints `10`.\n\nOutput: `2010`.\n\nA variable only lives until its closing `}`. After that, the outer variable is visible again."
    },
    {
      "id": 14,
      "text": "What is the output?\n```cpp\nint score = 42;\nint main() {\n    int score = 99;\n    std::cout << ::score << \" \" << score;\n}\n```",
      "options": {
        "A": "99 42",
        "B": "42 99",
        "C": "42 42",
        "D": "Compilation error"
      },
      "correct": ["B"],
      "mode": "any",
      "explanation": "There are two variables named `score` — one global (`42`), one local (`99`).\n\nThe local one **shadows** the global one. But you can still reach the global with **`::`**.\n\nStep by step:\n- `::score` → go to the global namespace → `42`\n- `score` → use the local one → `99`\n\nOutput: `42 99`.\n\n```cpp\n::score  // always means the global variable\nscore    // means the nearest (local) variable\n```"
    },
    {
      "id": 15,
      "text": "Which correctly reads a full line including spaces?\n```cpp\nstd::string str;\n// ???\nstd::cout << str;\n```",
      "options": {
        "A": "`std::cin >> str;`",
        "B": "`std::cin.get();`",
        "C": "`std::getline(std::cin, str);`",
        "D": "`std::cin.ignore(str);`"
      },
      "correct": ["C"],
      "mode": "any",
      "explanation": "**`std::getline(std::cin, str)`** reads the entire line, including spaces.\n\n`std::cin >> str` stops at the first space. It only gets one word.\n\n```cpp\n// Input: \"Hello World\"\nstd::cin >> str;              // str = \"Hello\"\nstd::getline(std::cin, str);  // str = \"Hello World\"\n```\n\nThe other options are wrong:\n- `std::cin.get()` reads a single character\n- `std::cin.ignore()` throws away characters, it does not store them"
    },
    {
      "id": 16,
      "text": "What is the output?\n```cpp\nint a = 3;\nint b = a++;\nstd::cout << a << \" \" << b;\n```",
      "options": {
        "A": "3 3",
        "B": "4 4",
        "C": "3 4",
        "D": "4 3"
      },
      "correct": ["D"],
      "mode": "any",
      "explanation": "`a++` is **post-increment**: use the value first, then add 1.\n\nStep by step:\n- `a = 3`\n- `b = a++` → `b` gets the old value `3`, then `a` becomes `4`\n- Print `a` → `4`, print `b` → `3`\n\nOutput: `4 3`.\n\nRemember:\n- `a++` (post) = use, then increment\n- `++a` (pre) = increment, then use"
    },
    {
      "id": 17,
      "text": "What is the output?\n```cpp\nint a = 3;\nint b = ++a;\nstd::cout << b << \" \" << a;\n```",
      "options": {
        "A": "4 4",
        "B": "3 4",
        "C": "4 3",
        "D": "3 3"
      },
      "correct": ["A"],
      "mode": "any",
      "explanation": "`++a` is **pre-increment**: add 1 first, then use the value.\n\nStep by step:\n- `a = 3`\n- `++a` → `a` becomes `4` first\n- `b = 4` (the new value)\n- Print `b` → `4`, print `a` → `4`\n\nOutput: `4 4`.\n\nCompare with post-increment:\n- `b = ++a` → both are `4` (increment first)\n- `b = a++` → `b` is `3`, `a` is `4` (use first, then increment)"
    },
    {
      "id": 18,
      "text": "What is the output?\n```cpp\nint i = 0;\nwhile (i < 6) {\n    i++;\n    if (i == 4) continue;\n    if (i == 6) break;\n    std::cout << i;\n}\n```",
      "options": {
        "A": "123456",
        "B": "1235",
        "C": "12345",
        "D": "01235"
      },
      "correct": ["B"],
      "mode": "any",
      "explanation": "Trace step by step (`i` starts at `0`):\n\n- `i` becomes `1` → not 4, not 6 → print `1`\n- `i` becomes `2` → not 4, not 6 → print `2`\n- `i` becomes `3` → not 4, not 6 → print `3`\n- `i` becomes `4` → `i == 4` is true → **`continue`** skips the rest, no print\n- `i` becomes `5` → not 4, not 6 → print `5`\n- `i` becomes `6` → `i == 6` is true → **`break`** exits the loop\n\nOutput: `1235`.\n\nKey rules:\n- `continue` = skip the rest of this round, go back to the loop condition\n- `break` = exit the loop completely"
    },
    {
      "id": 19,
      "text": "What is the output?\n```cpp\nint i = 0;\nwhile (i < 5) {\n    i++;\n    if (i % 2 == 0) continue;\n    std::cout << i;\n}\n```",
      "options": {
        "A": "12345",
        "B": "24",
        "C": "135",
        "D": "1234"
      },
      "correct": ["C"],
      "mode": "any",
      "explanation": "The `continue` skips even numbers. Trace step by step:\n\n- `i` becomes `1` → `1 % 2 == 0` is false → print `1`\n- `i` becomes `2` → `2 % 2 == 0` is true → **`continue`**, skip print\n- `i` becomes `3` → `3 % 2 == 0` is false → print `3`\n- `i` becomes `4` → `4 % 2 == 0` is true → **`continue`**, skip print\n- `i` becomes `5` → `5 % 2 == 0` is false → print `5`\n\nOutput: `135`. Only odd numbers are printed.\n\n`%` is the **modulo** operator. It gives the remainder.\n`n % 2 == 0` means the number is even."
    },
    {
      "id": 20,
      "text": "What is the output?\n```cpp\nint* getBadPointer() {\n    int x = 42;\n    return &x;\n}\nint main() {\n    int* p = getBadPointer();\n    std::cout << *p;\n}\n```",
      "options": {
        "A": "Always prints 42",
        "B": "Prints 0",
        "C": "Compilation error",
        "D": "Undefined behavior"
      },
      "correct": ["D"],
      "mode": "any",
      "explanation": "`x` is a local variable. It lives on the **stack** and is destroyed when the function returns.\n\nThe function returns `&x` — the address of `x`. But after the function ends, `x` is gone.\nNow `p` holds an address to memory that no longer exists. This is a **dangling pointer**.\n\nReading `*p` is **undefined behavior**. It might print 42, crash, or do something random.\n\nTwo ways to fix this:\n```cpp\n// Fix 1: return by value (best)\nint getValue() { return 42; }\n\n// Fix 2: use new (caller must delete)\nint* getPtr() { return new int(42); }\n```"
    },
    {
      "id": 21,
      "text": "What is the output?\n```cpp\nint* p;\n{\n    int x = 100;\n    p = &x;\n}\nstd::cout << *p;\n```",
      "options": {
        "A": "Undefined behavior",
        "B": "100",
        "C": "Compilation error",
        "D": "0"
      },
      "correct": ["A"],
      "mode": "any",
      "explanation": "`x` is local to the inner `{}` block. When the block ends, `x` is destroyed.\n\nBut `p` still holds the old address of `x`. This is a **dangling pointer**.\n\nReading `*p` is **undefined behavior** — the memory is no longer valid.\n\nSafe alternative — use `new` so the memory lives on the heap:\n```cpp\nint* p;\n{\n    p = new int(100);  // heap memory survives the block\n}\nstd::cout << *p;  // OK: prints 100\ndelete p;\n```"
    },
    {
      "id": 22,
      "text": "What does `std::cin.clear()` do after a failed input?",
      "options": {
        "A": "Empties all characters from the keyboard buffer",
        "B": "Resets the error flags on `cin` so it can be used again",
        "C": "Closes the input stream permanently",
        "D": "Deletes the last character typed"
      },
      "correct": ["B"],
      "mode": "any",
      "explanation": "When input fails (for example, you type `\"abc\"` but the program expects an `int`), `cin` enters a **fail state**.\n\nIn the fail state, all future reads are ignored. Nothing works until you fix it.\n\n**`std::cin.clear()`** resets the error flags so `cin` can work again.\n\nBut the bad input is still in the buffer. You also need `cin.ignore()` to remove it:\n```cpp\nstd::cin.clear();   // reset error flags\nstd::cin.ignore(1000, '\\n');  // remove bad input\nstd::cin >> n;      // now this works\n```"
    },
    {
      "id": 23,
      "text": "What is the output?\n```cpp\nint x = 5;\n{\n    int x = 10;\n    {\n        std::cout << x;\n    }\n}\nstd::cout << x;\n```",
      "options": {
        "A": "55",
        "B": "1010",
        "C": "105",
        "D": "510"
      },
      "correct": ["C"],
      "mode": "any",
      "explanation": "There are three nested scopes here. C++ always uses the **nearest** `x` it can find.\n\nStep by step:\n- The innermost `{}` has no `x` of its own. It looks outward and finds `x = 10` in the middle block. Prints `10`.\n- After both inner blocks close, those `x` variables are gone.\n- `std::cout << x` now finds the outermost `x = 5`. Prints `5`.\n\nOutput: `105`.\n\nRule: when the same name exists in multiple scopes, the **nearest enclosing scope** wins."
    },
    {
      "id": 24,
      "text": "What is the output?\n```cpp\nnamespace Physics { double g = 9.81; }\nnamespace Math { double pi = 3.14; }\nint main() {\n    std::cout << Physics::g - Math::pi;\n}\n```",
      "options": {
        "A": "12.95",
        "B": "0",
        "C": "Compilation error",
        "D": "6.67"
      },
      "correct": ["D"],
      "mode": "any",
      "explanation": "**Namespaces** group related names together. Use `::` to access them.\n\n- `Physics::g` = `9.81`\n- `Math::pi` = `3.14`\n- `9.81 - 3.14` = `6.67`\n\nOutput: `6.67`.\n\nNamespaces prevent name conflicts. Two namespaces can have the same variable name without problems:\n```cpp\nnamespace Audio { int rate = 44100; }\nnamespace Video { int rate = 60; }\n// Audio::rate and Video::rate are different variables\n```"
    },
    {
      "id": 25,
      "text": "What is wrong with this code?\n```cpp\nvoid foo() {\n    int* p = new int(1);\n    int* q = new int(2);\n    q = p;\n    delete q;\n}\n```",
      "options": {
        "A": "The integer with value 2 is leaked",
        "B": "Both integers are leaked",
        "C": "No memory is leaked",
        "D": "The integer with value 1 is leaked"
      },
      "correct": ["A"],
      "mode": "any",
      "explanation": "This is a **memory leak**.\n\nStep by step:\n- `p` points to `1` on the heap. `q` points to `2` on the heap.\n- `q = p` → now `q` points to `1`, same as `p`.\n- Nobody points to `2` anymore. That memory is lost forever — **leaked**.\n- `delete q` frees `1`. But `2` can never be freed.\n\nThe fix — delete before reassigning:\n```cpp\ndelete q;  // free 2 first\nq = p;     // now safe to reassign\n```"
    },
    {
      "id": 26,
      "text": "What is the output?\n```cpp\nint x = 0;\nint* p = &x;\n*p = 99;\nstd::cout << x << \" \" << *p;\n```",
      "options": {
        "A": "0 99",
        "B": "99 99",
        "C": "99 0",
        "D": "0 0"
      },
      "correct": ["B"],
      "mode": "any",
      "explanation": "`p` holds the **memory address** of `x`. They point to the same place.\n\nStep by step:\n- `int* p = &x` → `p` stores the address of `x`\n- `*p = 99` → write `99` to the address `p` holds → this changes `x` itself\n- Now `x` is `99` and `*p` is also `99` — same memory\n\nOutput: `99 99`.\n\n**Dereferencing** (`*p`) means \"go to the address and read or write the value there.\""
    },
    {
      "id": 27,
      "text": "What is the output?\n```cpp\nvoid setToTen(int* p) { *p = 10; }\nint main() {\n    int x = 0;\n    setToTen(&x);\n    std::cout << x;\n}\n```",
      "options": {
        "A": "0",
        "B": "Undefined behavior",
        "C": "10",
        "D": "Compilation error"
      },
      "correct": ["C"],
      "mode": "any",
      "explanation": "This is **pass by pointer**. The function receives the address of `x` and can change the original.\n\nStep by step:\n- `&x` gives the address of `x`\n- The function receives this address in `p`\n- `*p = 10` writes `10` to that address → `x` in `main()` becomes `10`\n\nOutput: `10`.\n\nThis is similar to **pass by reference** (`int&`), but uses pointer syntax.\nBoth let a function change the caller's variable."
    },
    {
      "id": 28,
      "text": "What is the output?\n```cpp\nnamespace Vehicle {\n    int speed = 100;\n    namespace Boat { int speed = 30; }\n}\nusing namespace Vehicle;\nint main() {\n    std::cout << speed << \" \" << Boat::speed;\n}\n```",
      "options": {
        "A": "30 100",
        "B": "100 100",
        "C": "Compilation error",
        "D": "100 30"
      },
      "correct": ["D"],
      "mode": "any",
      "explanation": "`using namespace Vehicle;` brings all of `Vehicle`'s members into the current scope.\n\nStep by step:\n- `speed` → finds `Vehicle::speed` → `100`\n- `Boat::speed` → finds `Vehicle::Boat::speed` → `30`\n\nOutput: `100 30`.\n\n**Important:** `using namespace` only brings in direct members. It does not bring in nested namespace members.\n\nSo `speed` works (direct member), but `Boat::speed` still needs the `Boat::` prefix.\nTo use the inner `speed` directly, you would also need `using namespace Vehicle::Boat;`."
    },
    {
      "id": 29,
      "text": "What is wrong with this code?\n```cpp\nvoid foo() {\n    int* a = new int(1);\n    int* b = new int(2);\n    delete a;\n}\n```",
      "options": {
        "A": "The integer with value 2 is leaked",
        "B": "The integer with value 1 is leaked",
        "C": "Both integers are leaked",
        "D": "No memory is leaked"
      },
      "correct": ["A"],
      "mode": "any",
      "explanation": "Every `new` must have a matching `delete`. Otherwise you have a **memory leak**.\n\n- `a` points to `1` → `delete a` frees it. Good.\n- `b` points to `2` → never deleted. When the function ends, the pointer `b` is gone, but the heap memory stays.\n\nThe integer `2` is **leaked**. No one can free it anymore.\n\nThe fix — add the missing `delete`:\n```cpp\ndelete a;\ndelete b;  // do not forget this\n```\n\nIn modern C++, use **`std::unique_ptr`** to avoid this problem. It deletes automatically."
    },
    {
      "id": 30,
      "text": "What is the output?\n```cpp\nint* p = new int(5);\nint* q = p;\ndelete p;\nstd::cout << *q;\n```",
      "options": {
        "A": "5",
        "B": "Undefined behavior",
        "C": "0",
        "D": "Compilation error"
      },
      "correct": ["B"],
      "mode": "any",
      "explanation": "`p` and `q` both point to the same `int` on the heap.\n\nAfter `delete p`, that memory is freed.\n`q` still holds the old address — this is a **dangling pointer**.\n\nReading `*q` is **undefined behavior**. It might print `5`, crash, or do something random.\n\nSafe pattern — set to `nullptr` after delete:\n```cpp\ndelete p;\np = nullptr;\nq = nullptr;\n```"
    }
  ]
}
