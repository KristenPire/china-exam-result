{
  "bodyType": "mcq",
  "questions": [
    {
      "id": 1,
      "text": "In the 1950s, the OS was just a library. What was the biggest danger?",
      "options": {
        "A": "Programs ran too slowly with no scheduler",
        "B": "Library functions were incompatible across machines",
        "C": "Any program could read or write any memory",
        "D": "Programs could not share the CPU at all"
      },
      "correct": [
        "C"
      ],
      "mode": "any",
      "explanation": "No memory protection hardware existed in the 1950s — OS was just a library running alongside user programs.\n\nAny program could read or write *any* address, including the OS itself. One buggy program = corrupted system.\n\nModern CPUs added the MMU to enforce isolation. Without it, C is the correct danger."
    },
    {
      "id": 2,
      "text": "In batch processing, an operator ran jobs one by one. What was the main problem?",
      "options": {
        "A": "Programs could not be longer than one punch card",
        "B": "Users had to wait hours or days for results",
        "C": "Only one user account existed per computer",
        "D": "The operator had to check every result manually"
      },
      "correct": [
        "B"
      ],
      "mode": "any",
      "explanation": "Batch = submit punch cards → collected into a batch → run overnight → results next day.\n\n```\nSubmit Friday → find your typo Monday\n```\n\nThis painful wait drove the invention of interactive **time-sharing**: give each user a fast slice of the machine."
    },
    {
      "id": 3,
      "text": "Which statements about multiprogramming are correct?",
      "options": {
        "A": "The CPU runs another job while one waits for disk",
        "B": "Multiple programs run in parallel inside one CPU core",
        "C": "Programs in memory can read each other's data directly",
        "D": "CPU utilization improves because idle time is reduced"
      },
      "correct": [
        "A",
        "D"
      ],
      "mode": "all",
      "explanation": "Multiprogramming overlaps computation and I/O:\n\n```\nJob 1: ──CPU──|── I/O wait ──────────|──CPU──\nJob 2:        |──────CPU─────────────|\n                 ↑ CPU stays busy\n```\n\n**A ✓** While Job 1 waits for disk, CPU runs Job 2.\n**D ✓** Multiple jobs in memory → CPU rarely idle → better utilization.\n**B ✗** One core = no true parallelism. Jobs interleave, not overlap on the same core.\n**C ✗** Programs have isolated address spaces — no shared variables."
    },
    {
      "id": 4,
      "text": "What was the main design idea of UNIX?",
      "options": {
        "A": "One large program that manages all hardware",
        "B": "Small tools that each do one task well",
        "C": "A graphical window system for all users",
        "D": "Separate code written for each type of machine"
      },
      "correct": [
        "B"
      ],
      "mode": "any",
      "explanation": "UNIX philosophy: *do one thing well*, then compose with pipes.\n\n```\ngrep ERROR log.txt | sort | uniq -c\n```\n\nEach tool is small and simple. Power comes from composition.\n**A** describes a monolithic design, not UNIX philosophy.\n**C** is wrong — UNIX was terminal-only; X11 came much later.\n**D** is wrong — UNIX was designed for *portability* (opposite of machine-specific)."
    },
    {
      "id": 5,
      "text": "`counter = 5`. Thread A loads 5. Before A stores, Thread B also loads 5. Both add 1 and store. What is the final value of `counter`?",
      "options": {
        "A": "6 — one increment was lost in the overlap",
        "B": "7 — each thread worked on its own separate copy",
        "C": "5 — the two stores cancelled each other out",
        "D": "11 — both threads each counted their own addition twice"
      },
      "correct": [
        "A"
      ],
      "mode": "any",
      "explanation": "```\nThread A: LOAD counter  → gets 5\nThread B: LOAD counter  → gets 5   ← races before A stores\nThread A: ADD 1 → 6, STORE 6\nThread B: ADD 1 → 6, STORE 6\n                             ↑ second write silently lost\n```\n\nBoth threads read 5, both compute 6, both store 6. Result: **6**, not 7.\n\nFix: mutex lock around read-modify-write to make it atomic."
    },
    {
      "id": 6,
      "text": "Which is an example of a **mechanism**, not a policy?",
      "options": {
        "A": "Saving and restoring CPU registers during a switch",
        "B": "Choosing the job with the shortest remaining time",
        "C": "Running the job that has waited the longest first",
        "D": "Deciding which process gets the CPU next"
      },
      "correct": [
        "A"
      ],
      "mode": "any",
      "explanation": "**Mechanism** = *how* to do something (the low-level procedure)\n**Policy** = *what* to do / *which* to choose (the decision)\n\nSaving/restoring registers is the *how* of context switching — no decision is made.\nB, C, D all describe *which job to pick* = policy."
    },
    {
      "id": 7,
      "text": "Which are main OS responsibilities?",
      "options": {
        "A": "Compiling source code into binary programs",
        "B": "Safely handling many operations at the same time",
        "C": "Routing network packets between computers",
        "D": "Keeping data safe after the computer loses power"
      },
      "correct": [
        "B",
        "D"
      ],
      "mode": "all",
      "explanation": "The OS's three core responsibilities: **Virtualization**, **Concurrency**, **Persistence**.\n\n**B ✓** Safe handling of simultaneous operations = Concurrency (locks, atomic ops)\n**D ✓** Keeping data safe across power loss = Persistence (journaling, fsync)\n**A ✗** Compiling is the *compiler's* job (gcc, clang) — OS just executes binaries\n**C ✗** Network routing is done by routers and the network stack"
    },
    {
      "id": 8,
      "text": "A file is corrupted after a crash during writing. Which OS responsibility failed?",
      "options": {
        "A": "Resource Management — CPU was not shared fairly",
        "B": "Concurrency — two threads wrote to the same file",
        "C": "Persistence — data was not safely stored on disk",
        "D": "Virtualization — the memory mapping was incorrect"
      },
      "correct": [
        "C"
      ],
      "mode": "any",
      "explanation": "Power loss mid-write → file partially written → corrupted.\n\nThis is a **Persistence** failure: the file system must guarantee durability and consistency even during crashes.\n\n```\nSolutions: journaling, copy-on-write, fsync()\n```\n\nA and B describe concurrency issues (not relevant here — one thread, no race).\nD is a memory mapping issue, unrelated to disk durability."
    },
    {
      "id": 9,
      "text": "Program A and B both use virtual address `0x1000`. Which statement is true?",
      "options": {
        "A": "Both share the same physical memory cell at `0x1000`",
        "B": "The OS copies data between them at each context switch",
        "C": "Each `0x1000` maps to a different physical location",
        "D": "Only one program at a time can use address `0x1000`"
      },
      "correct": [
        "C"
      ],
      "mode": "any",
      "explanation": "Virtual memory: each process gets its own private address space.\n\n```\nProcess A: virtual 0x1000 ──► physical 0x4A000  (A's page table)\nProcess B: virtual 0x1000 ──► physical 0x7F000  (B's page table)\n```\n\nThe MMU translates independently per process. Writing to 0x1000 in A has zero effect on B."
    },
    {
      "id": 10,
      "text": "Three programs appear to run at the same time on one CPU. How?",
      "options": {
        "A": "All three run together inside the OS kernel",
        "B": "The CPU speed is split equally among all three",
        "C": "Two run on the CPU; the third waits on disk",
        "D": "The OS switches between them faster than humans notice"
      },
      "correct": [
        "D"
      ],
      "mode": "any",
      "explanation": "CPU time-slicing creates the illusion of parallelism:\n\n```\nP1: ██  ·  ·  ██  ·  ·  ██\nP2:  ·  ██  ·   ·  ██  ·\nP3:  ·   ·  ██  ·   ·  ██\n    ─────────────────────► time\n```\n\nEach program runs at full speed during its slice (~10ms). At ~100 switches/second, humans perceive smooth simultaneity. This is **CPU virtualization**."
    },
    {
      "id": 11,
      "text": "Which statements about kernel mode and user mode are correct?",
      "options": {
        "A": "In user mode, programs can directly access any hardware",
        "B": "In kernel mode, the OS can access any memory or device",
        "C": "In user mode, programs must use system calls for services",
        "D": "In kernel mode, the OS runs slower due to extra checks"
      },
      "correct": [
        "B",
        "C"
      ],
      "mode": "all",
      "explanation": "Two hardware privilege levels:\n\n```\nUser mode   → restricted: no direct hardware access, no privileged instructions\nKernel mode → unrestricted: full access to all memory and all hardware\n```\n\n**B ✓** Kernel mode = unrestricted access to everything\n**C ✓** User programs must go through system calls (the controlled gateway)\n**A ✗** User mode *cannot* directly access hardware — that's the whole point\n**D ✗** Kernel mode runs at the same clock speed, just with fewer restrictions"
    },
    {
      "id": 12,
      "text": "A user program makes a system call. What happens to the CPU mode?",
      "options": {
        "A": "The CPU switches from user mode to kernel mode",
        "B": "The CPU stays in user mode but runs the call faster",
        "C": "The CPU queues the request and handles it later",
        "D": "The CPU creates a second thread to handle the call"
      },
      "correct": [
        "A"
      ],
      "mode": "any",
      "explanation": "A system call is the controlled gateway from user mode to kernel mode.\n\n```\nUser mode ──trap──► Kernel mode ──return-from-trap──► User mode\n              ↑                   ↑\n        save registers       restore registers\n        jump to handler       return to program\n```\n\nThe CPU *must* switch to kernel mode to execute privileged operations (disk I/O, network, etc.)."
    },
    {
      "id": 13,
      "text": "What is a process?",
      "options": {
        "A": "A program file stored on disk, waiting to run",
        "B": "A running program loaded into memory and executing",
        "C": "A hardware interrupt signal sent to the CPU",
        "D": "A kernel thread running on behalf of a user"
      },
      "correct": [
        "B"
      ],
      "mode": "any",
      "explanation": "**Program** = static binary file on disk\n**Process** = that program loaded into memory and *running*\n\nA process has: program counter, CPU registers, stack (locals), heap (dynamic data), and state (RUNNING/READY/BLOCKED).\n\nOne Firefox binary → open 3 windows → 3 separate processes with independent state."
    },
    {
      "id": 14,
      "text": "Firefox is opened three times. How many programs and processes are there?",
      "options": {
        "A": "1 program and 3 processes",
        "B": "3 programs and 3 processes",
        "C": "3 programs and 1 process",
        "D": "1 program and 1 process"
      },
      "correct": [
        "A"
      ],
      "mode": "any",
      "explanation": "```\nfirefox binary on disk: 1 program\n3 firefox windows:      3 processes (each with own PID, stack, heap)\n\nprogram (passive, disk) → load → process (active, memory)\n                                 × 3 instances\n```\n\nThe binary isn't duplicated — just instantiated multiple times."
    },
    {
      "id": 15,
      "text": "A process runs `int* p = new int[100];`. Where is this memory in the address space?\n\n```\nHIGH ADDRESS\n┌──────────────┐\n│    Stack     │\n│    (free)    │\n│    Heap      │\n├──────────────┤\n│  Static Data │\n├──────────────┤\n│     Code     │\n└──────────────┘\nLOW ADDRESS\n```",
      "options": {
        "A": "Code — it is part of the program binary",
        "B": "Stack — it is allocated inside a function call",
        "C": "Static Data — it is a variable used globally",
        "D": "Heap — `new` allocates dynamic memory at runtime"
      },
      "correct": [
        "D"
      ],
      "mode": "any",
      "explanation": "```\nHIGH ADDRESS\n┌──────────────┐\n│    Stack     │  ← local variables, function frames (auto-freed)\n│    (free)    │\n│    Heap      │  ← malloc(), new, calloc  ← p lands here\n├──────────────┤\n│  Static Data │  ← global/static variables\n├──────────────┤\n│     Code     │  ← compiled instructions\n└──────────────┘\nLOW ADDRESS\n```\n\n`new` = explicit runtime allocation → **heap**. Unlike stack variables, heap memory persists beyond the function that created it."
    },
    {
      "id": 16,
      "text": "Why does P1 change from READY to RUNNING at time 4?\n\n```\nTime | P0        | P1\n  1  | RUNNING   | READY\n  2  | RUNNING   | READY\n  3  | RUNNING   | READY   ← P0 calls read() from disk\n  4  | BLOCKED   | RUNNING\n```",
      "options": {
        "A": "P0 finished all its work and exited at time 3",
        "B": "P1 has a higher priority than P0 in this system",
        "C": "P0 is blocked on disk, so the OS runs P1 instead",
        "D": "A timer fired and the OS randomly selected P1"
      },
      "correct": [
        "C"
      ],
      "mode": "any",
      "explanation": "P0 called `read()` from disk at t=3 → slow blocking I/O → moves to BLOCKED.\n\nOS: P0 is blocked, P1 is READY → schedule P1.\n\nThis is **I/O-triggered context switching** — the core of multiprogramming.\nP0 didn't exit: it's waiting for disk to respond. It returns to RUNNING when I/O completes."
    },
    {
      "id": 17,
      "text": "Which items are stored in the Process Control Block (PCB)?",
      "options": {
        "A": "The source code of the program before compilation",
        "B": "The saved CPU registers when the process is not running",
        "C": "A list of all users currently logged into the system",
        "D": "The current process state such as READY or RUNNING"
      },
      "correct": [
        "B",
        "D"
      ],
      "mode": "all",
      "explanation": "The **PCB (Process Control Block)** is the OS's complete record of a process.\n\n**B ✓** Saved CPU registers — when descheduled, all register values go here so execution resumes exactly where it left off\n**D ✓** Current state (RUNNING/READY/BLOCKED) — tells the scheduler who can run\n**A ✗** Source code lives in the developer's repo, not the PCB\n**C ✗** Logged-in users is a system-wide table, not per-process data"
    },
    {
      "id": 18,
      "text": "Which outputs are possible when this code runs? (fork() succeeds)\n\n```cpp\nint rc = fork();\nif (rc == 0) {\n    std::cout << \"child\" << std::endl;\n} else {\n    std::cout << \"parent\" << std::endl;\n}\n```",
      "options": {
        "A": "\"child\" then \"parent\"",
        "B": "\"parent\" then \"child\"",
        "C": "only \"child\" — parent exits right after fork()",
        "D": "only \"parent\" — child never reaches the output line"
      },
      "correct": [
        "A",
        "B"
      ],
      "mode": "all",
      "explanation": "After `fork()`, two independent processes exist — the scheduler decides execution order.\n\n```\nChild:  rc == 0  → prints \"child\"\nParent: rc == 920 → prints \"parent\"\n```\n\nBoth print exactly once. Which runs *first* is up to the OS scheduler:\n\n```\nPossible 1: child first  → \"child\\nparent\"\nPossible 2: parent first → \"parent\\nchild\"\n```\n\nBoth orderings are valid. There is no guaranteed order after fork."
    },
    {
      "id": 19,
      "text": "A parent calls fork(). The return value in the **parent** is 920. What does 920 represent?",
      "options": {
        "A": "The time in ms that fork() needed to complete",
        "B": "The amount of memory copied to the child process",
        "C": "The process ID assigned to the new child process",
        "D": "The number of total processes running right now"
      },
      "correct": [
        "C"
      ],
      "mode": "any",
      "explanation": "`fork()` returns different values to the two processes it creates:\n\n```\nParent → child's PID (e.g., 920)\nChild  → 0\nError  → -1\n```\n\nThe parent uses this PID to manage the child: `waitpid(920, ...)` or `kill(920, SIGTERM)`."
    },
    {
      "id": 20,
      "text": "Program A runs an infinite loop, never calls a system call. Cooperative scheduling only. What happens?",
      "options": {
        "A": "The OS kills the program after about one second",
        "B": "Program A runs forever — other programs cannot run",
        "C": "A timer fires after 10ms and switches programs",
        "D": "Program A yields the CPU when the loop counter overflows"
      },
      "correct": [
        "B"
      ],
      "mode": "any",
      "explanation": "Cooperative scheduling relies on programs voluntarily yielding the CPU via system calls or `yield()`.\n\n```cpp\nwhile (true) { /* no system calls */ }\n             ↑\n       OS never gets CPU back → system freezes\n```\n\nThe only recovery is a hard reboot.\n**C** describes *preemptive* scheduling (timer-based), invented specifically to fix this problem."
    },
    {
      "id": 21,
      "text": "A timer fires while Process A runs. Which sequence is correct?\n\n```\nP: OS decides to switch to Process B\nQ: Timer fires\nR: Hardware saves A's registers to kernel stack\nS: OS loads B's registers and jumps to B\nT: CPU jumps to the OS interrupt handler\n```",
      "options": {
        "A": "Q → R → T → P → S",
        "B": "Q → T → R → P → S",
        "C": "R → Q → T → P → S",
        "D": "Q → P → R → T → S"
      },
      "correct": [
        "A"
      ],
      "mode": "any",
      "explanation": "Timer interrupts have two phases: **hardware** acts first, then **OS software**.\n\n```\nQ: Timer fires (hardware event)\nR: Hardware saves A's registers → kernel stack   [automatic]\nT: CPU jumps to OS interrupt handler, kernel mode\nP: OS decides to switch to Process B             [software]\nS: OS loads B's registers, jumps to B            [software]\n```\n\nHardware (Q→R→T) completes before the OS runs even one instruction.\n**C** (R before Q) is impossible — can't save registers before the timer fires.\n**D** (OS decides before kernel mode) is impossible — OS has no CPU yet."
    },
    {
      "id": 22,
      "text": "During a context switch, who performs the second register save (kernel registers → PCB)?",
      "options": {
        "A": "The OS software inside the context switch routine",
        "B": "The hardware automatically when the interrupt fires",
        "C": "The user program by calling a built-in save function",
        "D": "The disk driver while finishing the current I/O job"
      },
      "correct": [
        "A"
      ],
      "mode": "any",
      "explanation": "Two register saves happen during a context switch:\n\n```\n1st: Hardware saves user registers → kernel stack  (triggered by timer/trap)\n2nd: OS software saves kernel registers → PCB A    (written in assembly)\n     Then: OS loads PCB B's registers → resumes B\n```\n\nThe OS does the 2nd save explicitly — the hardware can't know *which* process's PCB to write to."
    },
    {
      "id": 23,
      "text": "Which are hidden costs of a context switch?",
      "options": {
        "A": "The process must reload its full code from disk",
        "B": "The CPU cache now holds data from the old process",
        "C": "The OS must recompile the program before it resumes",
        "D": "The TLB must be refilled for the new process's addresses"
      },
      "correct": [
        "B",
        "D"
      ],
      "mode": "all",
      "explanation": "Beyond register save/restore, switching has *hidden* performance costs:\n\n**B ✓ Cache pollution**\nOld process filled L1/L2 cache with its data. New process's data is cold → cache misses (~100ns vs ~1ns) until warm.\n\n**D ✓ TLB flush**\nTLB caches virtual→physical translations. Different processes have different mappings → TLB invalid for new process → page-table walks until refilled.\n\n**A ✗** Code stays in RAM — no disk reload on switch.\n**C ✗** Programs are compiled once, never recompiled at runtime."
    },
    {
      "id": 24,
      "text": "Why must only the OS kernel be allowed to control the hardware timer?",
      "options": {
        "A": "Timer circuits require low-level assembly code to operate",
        "B": "A user program that stops the timer could run forever uninterrupted",
        "C": "The timer is the OS's only way to forcibly take back the CPU",
        "D": "Two programs sharing timer control would cause hardware conflicts"
      },
      "correct": [
        "B",
        "C"
      ],
      "mode": "all",
      "explanation": "The hardware timer is the foundation of preemptive scheduling — and must be protected.\n\n**B ✓** If a user program disables the timer, the OS can never interrupt it:\n```\nUser program → disables timer → infinite loop → OS never runs → frozen\n```\n\n**C ✓** The timer interrupt is the OS's *only* hardware mechanism to forcibly take the CPU back.\n\n**A ✗** It's a privilege issue, not a programming difficulty issue.\n**D ✗** Timer-sharing causes scheduling chaos, but it's not a hardware accuracy problem."
    },
    {
      "id": 25,
      "text": "Turnaround time is:",
      "options": {
        "A": "T_completion − T_arrival",
        "B": "T_firstrun − T_arrival",
        "C": "T_arrival − T_completion",
        "D": "T_completion − T_firstrun"
      },
      "correct": [
        "A"
      ],
      "mode": "any",
      "explanation": "Two key scheduling metrics from OSTEP Chapter 7:\n\n```\nTurnaround = T_completion − T_arrival  (total time: arrival → done)\nResponse   = T_firstrun  − T_arrival  (time until first CPU access)\n```\n\n**B** is the *response time* formula, not turnaround.\n**C** is backwards — would give a negative number.\n**D** is execution duration — not a standard metric."
    },
    {
      "id": 26,
      "text": "[Calculation] Three jobs arrive at time 0: A=10s, B=10s, C=10s. FIFO: A→B→C. Average turnaround time?",
      "options": {
        "A": "10s",
        "B": "15s",
        "C": "30s",
        "D": "20s"
      },
      "correct": [
        "D"
      ],
      "mode": "any",
      "explanation": "```\n|── A (10s) ──|── B (10s) ──|── C (10s) ──|\nt=0           t=10          t=20           t=30\n\nA: 10−0 = 10s  |  B: 20−0 = 20s  |  C: 30−0 = 30s\nAverage = (10 + 20 + 30) / 3 = 20s\n```\n\nWith equal-length jobs, FIFO is optimal — ordering doesn't matter. The problem appears when job lengths differ."
    },
    {
      "id": 27,
      "text": "[Calculation] Three jobs arrive at time 0: A=100s, B=10s, C=10s. FIFO: A→B→C. Average turnaround time?",
      "options": {
        "A": "40s",
        "B": "110s",
        "C": "70s",
        "D": "130s"
      },
      "correct": [
        "B"
      ],
      "mode": "any",
      "explanation": "```\n|──────── A (100s) ────────|── B ──|── C ──|\nt=0                         t=100  t=110   t=120\n\nA: 100s  |  B: 110s  |  C: 120s\nAverage = (100 + 110 + 120) / 3 = 110s\n```\n\nThis is the **convoy effect**: B and C (10s work) spend 90+ seconds waiting behind A.\nSJF would order B→C→A → average = (10 + 20 + 120)/3 = 50s — a 2× improvement."
    },
    {
      "id": 28,
      "text": "[Calculation] Three jobs arrive at time 0: A=10s, B=5s, C=15s. SJF (shortest first). What is the **response time** for job A?",
      "options": {
        "A": "0s",
        "B": "5s",
        "C": "10s",
        "D": "15s"
      },
      "correct": [
        "B"
      ],
      "mode": "any",
      "explanation": "Jobs: A=10s, B=5s, C=15s — SJF orders shortest first:\n\n```\n|── B (5s) ──|──── A (10s) ────|──────── C (15s) ────────|\nt=0           t=5               t=15                       t=30\n```\n\n**Response time for A = T_firstrun − T_arrival = 5 − 0 = 5s**\n\nA's first run starts at t=5 (after B finishes). It arrived at t=0.\n**A (0s)** — wrong, B goes first (shorter). **C (10s)** — that's A's duration. **D (15s)** — that's A's completion time."
    },
    {
      "id": 29,
      "text": "[Calculation] Round Robin, time slice = 3s, 4 jobs all arriving at time 0. Worst-case response time?",
      "options": {
        "A": "3s",
        "B": "6s",
        "C": "12s",
        "D": "9s"
      },
      "correct": [
        "D"
      ],
      "mode": "any",
      "explanation": "Round Robin worst-case response formula: **(N − 1) × time_slice**\n\n```\nt=0:  Job 1 gets CPU  (response = 0s)\nt=3:  Job 2 gets CPU  (response = 3s)\nt=6:  Job 3 gets CPU  (response = 6s)\nt=9:  Job 4 gets CPU  (response = 9s)\n```\n\nWorst-case = (4 − 1) × 3 = **9s**\n\nThe last job must wait for all N−1 others to complete their first slice."
    },
    {
      "id": 30,
      "text": "Why can the OS not use SJF for real programs?",
      "options": {
        "A": "SJF uses too much memory to sort all waiting jobs",
        "B": "SJF only works when all jobs arrive at the same time",
        "C": "SJF needs special hardware most computers do not have",
        "D": "The OS cannot know in advance how long each job runs"
      },
      "correct": [
        "D"
      ],
      "mode": "any",
      "explanation": "SJF requires knowing each job's run time *before* it starts:\n\n```\nText editor:  30 seconds or 3 hours?  (depends on user input)\nWeb request:  1ms or 10s?             (depends on query)\nCompiler:     exits early or runs for minutes?\n```\n\nImpossible to know in general. MLFQ approximates SJF by *observing* behavior over time instead of requiring advance knowledge."
    },
    {
      "id": 31,
      "text": "Which statements about Round Robin are correct?",
      "options": {
        "A": "Round Robin gives very low average turnaround time",
        "B": "Round Robin gives each job a fast first turn",
        "C": "Round Robin must know each job's total length first",
        "D": "Round Robin shares the CPU using a fixed time slice"
      },
      "correct": [
        "B",
        "D"
      ],
      "mode": "all",
      "explanation": "**B ✓** Fast first turn — every job gets CPU within (N−1) × slice\n**D ✓** Fixed time quantum — the defining mechanism of RR\n**A ✗** RR has *poor* turnaround — interleaving delays everyone's completion\n**C ✗** RR needs no foreknowledge — just rotates blindly (advantage over SJF)\n\nRR trade-off: **great response time, poor turnaround time**."
    },
    {
      "id": 32,
      "text": "A job uses 2ms of CPU then does I/O. Allotment per level = 10ms. After I/O, what is its priority?",
      "options": {
        "A": "Reduced — the job used some CPU at this level",
        "B": "Moved to lowest — I/O jobs always go to the bottom",
        "C": "Reset to top — finishing I/O counts as a new arrival",
        "D": "Unchanged — the job did not use its full allotment"
      },
      "correct": [
        "D"
      ],
      "mode": "any",
      "explanation": "```\nAllotment:  10ms\nUsed:        2ms\nRemaining:   8ms  ← still in the account\n```\n\nReturns from I/O → re-enters at the **same queue** with 8ms left.\n\nDemotion only happens when the full allotment is *exhausted*. I/O doesn't reset or consume the remaining allotment."
    },
    {
      "id": 33,
      "text": "A CPU-bound job enters MLFQ. Q2 (high) → Q1 → Q0 (low). Allotment = 10ms. No I/O. After 30ms, which queue?",
      "options": {
        "A": "Q2 — CPU use alone does not reduce priority",
        "B": "Q1 — only demoted once after using Q2's allotment",
        "C": "Q0 — demoted twice: Q2 → Q1, then Q1 → Q0",
        "D": "Q1 — reaching Q0 requires more than 20ms total"
      },
      "correct": [
        "C"
      ],
      "mode": "any",
      "explanation": "```\nt=0:  enters Q2 ──── 10ms ──► allotment exhausted → demoted to Q1\nt=10: at Q1 ──────── 10ms ──► allotment exhausted → demoted to Q0\nt=20: at Q0 ──────── 10ms ──► still Q0\nt=30: ► sitting in Q0\n```\n\nTwo demotions: Q2→Q1→Q0. MLFQ *learned* this job is CPU-bound by watching it exhaust allotments."
    },
    {
      "id": 34,
      "text": "Without priority boost, which problem occurs?",
      "options": {
        "A": "New jobs always start at the bottom and wait too long",
        "B": "Interactive jobs slowly sink by using too much CPU",
        "C": "The scheduler switches queues too often and wastes time",
        "D": "Long-running jobs at the bottom may never get CPU time"
      },
      "correct": [
        "D"
      ],
      "mode": "any",
      "explanation": "Without periodic boost, CPU-bound jobs at the bottom **starve**:\n\n```\nQ2: [interactive A] [interactive B] ← always something here\nQ1: [batch job X]                   ← runs sometimes\nQ0: [long job Y]                    ← NEVER RUNS\n```\n\nPriority boost (Rule 5) periodically moves *all* jobs to Q2 — guaranteeing everyone gets CPU time eventually."
    },
    {
      "id": 35,
      "text": "A long-running job has been sitting in Q0 for a long time. Then a priority boost fires. Where does the job go, and why?",
      "options": {
        "A": "It stays in Q0 — priority boost only affects new jobs entering the system",
        "B": "It moves to Q1 — each boost moves a job up exactly one level",
        "C": "It moves to Q2 — all jobs are reset to the highest queue regardless of history",
        "D": "It is terminated — the OS removes jobs that have been in Q0 too long"
      },
      "correct": [
        "C"
      ],
      "mode": "any",
      "explanation": "**MLFQ Rule 5:** After time period S, move ALL jobs to the top queue.\n\n```\nQ0: [long job Y]    ──boost──►  Q2: [long job Y]\nQ1: [batch X]       ──boost──►  Q2: [batch X]\nQ2: [interactive A] ──boost──►  Q2: [interactive A]\n```\n\n\"All\" means all — directly to Q2, not one level up. Not just new jobs. *Every* job.\n\nTwo benefits: prevents starvation + recalibrates jobs that changed behavior."
    },
    {
      "id": 36,
      "text": "Which statements about MLFQ are correct?",
      "options": {
        "A": "New jobs enter at the lowest priority queue by default",
        "B": "CPU-heavy jobs slowly move to lower priority queues",
        "C": "A periodic boost moves all jobs back to the top queue",
        "D": "All jobs stay in the same queue their entire run"
      },
      "correct": [
        "B",
        "C"
      ],
      "mode": "all",
      "explanation": "**B ✓** CPU-heavy jobs exhaust allotment at each level → Q2→Q1→Q0\n**C ✓** Periodic boost moves all jobs to Q2 → prevents starvation\n**A ✗** New jobs enter at the *highest* queue (Q2) — benefit of the doubt, not the bottom\n**D ✗** Jobs move constantly: CPU-bound jobs sink, boosts push everyone up"
    },
    {
      "id": 37,
      "text": "A job at Q2 uses 6ms of CPU, then does I/O. Allotment per level = 10ms. When it comes back from I/O, how much allotment remains at Q2?",
      "options": {
        "A": "10ms — the allotment resets after every I/O call",
        "B": "4ms — the OS tracks total CPU used, even across I/O",
        "C": "0ms — doing I/O counts as using the remaining allotment",
        "D": "6ms — the previous usage is forgotten when I/O starts"
      },
      "correct": [
        "B"
      ],
      "mode": "any",
      "explanation": "```\nUsed:  6ms of 10ms allotment\nLeft:  10 − 6 = 4ms\n```\n\nReturns from I/O → same level (Q2), **4ms remaining**.\nUses those 4ms → exhausted → demoted to Q1.\n\n**A (10ms)** — full reset only at priority boost, not after every I/O.\n**C (0ms)** — I/O doesn't consume remaining allotment.\n**D (6ms)** — prior usage is *remembered* to prevent gaming the scheduler."
    },
    {
      "id": 38,
      "text": "Why does every new job start at the highest priority queue in MLFQ?",
      "options": {
        "A": "New jobs are always more urgent than existing ones",
        "B": "New jobs need more CPU time than jobs already running",
        "C": "The OS assumes the job is short until it proves otherwise",
        "D": "Starting high gives the user a chance to set the priority"
      },
      "correct": [
        "C"
      ],
      "mode": "any",
      "explanation": "MLFQ has no information about a new job's behavior at arrival.\n\n```\nAssumption: probably short/interactive → start at Q2 (optimistic)\n\nIf right  → job finishes quickly at high priority → great response time\nIf wrong  → job exhausts allotments → demoted naturally → MLFQ learned\n```\n\nThis is how MLFQ approximates SJF without any foreknowledge."
    },
    {
      "id": 39,
      "text": "Job A uses 1ms of CPU then does I/O, repeatedly. Job B uses CPU non-stop. After several rounds, which job stays at the highest priority?",
      "options": {
        "A": "B — it uses more total CPU time overall",
        "B": "Both — MLFQ keeps all active jobs at the same level",
        "C": "A — it never uses its full allotment at any level",
        "D": "Neither — both sink to the bottom after enough time"
      },
      "correct": [
        "C"
      ],
      "mode": "any",
      "explanation": "```\nJob A (1ms CPU + I/O, repeating):\n  uses 1ms at Q2 → I/O → returns to Q2 with 9ms left → repeat\n  never exhausts allotment → stays at Q2 ✓\n\nJob B (pure CPU):\n  uses 10ms at Q2 → demoted to Q1\n  uses 10ms at Q1 → demoted to Q0\n  stays at Q0\n```\n\nA gets great response time. This is MLFQ's emergent behavior — designed to reward interactive patterns."
    },
    {
      "id": 40,
      "text": "MLFQ tries to achieve two goals at once. Which are they?",
      "options": {
        "A": "Give all jobs exactly equal CPU time over any period",
        "B": "Good turnaround time by running shorter jobs first",
        "C": "Good response time for interactive and short jobs",
        "D": "Maximum memory usage across all running processes"
      },
      "correct": [
        "B",
        "C"
      ],
      "mode": "all",
      "explanation": "MLFQ solves a hard trade-off between two scheduling goals:\n\n**B ✓ Good turnaround time** — detect CPU-bound jobs and demote them, so short jobs run first (like SJF without foreknowledge)\n**C ✓ Good response time** — interactive/I/O jobs stay at high priority → prompt CPU access\n\n**A ✗** Equal CPU time is *Round Robin's* goal — MLFQ is deliberately unequal based on observed behavior.\n**D ✗** Memory usage is a separate concern entirely."
    }
  ]
}